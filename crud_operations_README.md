CRUD Operations

## Overview
CRUD represents basic database operations.

## Operations

- Create → POST "user gives data as request and then get's an response from the server through api"
- Read → GET  "when you won't only the data"
- Update → PUT "when you won't to add new data or updata current data, you have th egive full field data even if only one change"
- Delete → DELETE "delete the request data according to rthe conditons"
-PATCH → we use this when we have to update just one column of whole data

## Example

POST /students → create  
GET /students → read  
PUT /students/1 → update  
DELETE /students/1 → delete  

## Key Idea
CRUD operations form the backbone of backend systems.